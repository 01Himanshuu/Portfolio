/**
 * Design System — Barrel Export
 *
 * Import any DS primitive from a single path:
 *   import { SectionContainer, MacWindow, Display, Label, EASE, DURATION } from '../ds';
 */

export { default as SectionContainer } from './SectionContainer';
export { default as MacWindow } from './MacWindow';
export {
  Display,
  SectionTitle,
  ProjectTitle,
  Body,
  Meta,
  Label,
} from './EditorialHeading';
export {
  EASE,
  DURATION,
  SCRUB,
  STAGGER,
  TRIGGER,
} from './motion';
